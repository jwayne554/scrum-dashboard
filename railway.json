{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run install:all && cd server && npx prisma generate && cd .. && npm run build && cd server && npx prisma migrate deploy"
  },
  "deploy": {
    "startCommand": "cd server && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}